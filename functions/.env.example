# Secret used to sign and validate booking QR tokens.
QR_SECRET=replace-with-strong-random-secret

# Firebase project ID used by local scripts/emulator helpers.
PROJECT_ID=replace-with-firebase-project-id

# Set true only when intentionally running local backfill scripts.
RUN_BACKFILL=false

# Set true when running Firebase Functions in the emulator.
FUNCTIONS_EMULATOR=false

# Didit webhook signing secret from the Didit Console.
# Required only when diditVerificationWebhook is deployed/enabled.
DIDIT_WEBHOOK_SECRET_KEY=replace-with-didit-webhook-secret

# Not required for the current Flutter Method 2 Didit flow:
# DIDIT_API_KEY and DIDIT_WORKFLOW_ID are not used by Functions because the
# mobile app creates Didit sessions directly with startVerificationWithWorkflow.
